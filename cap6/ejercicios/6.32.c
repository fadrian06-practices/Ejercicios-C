/*
  (Ordenamiento por selección.) Un ordenamiento por selección busca un arreglo
  que busca al elemento más pequeño del arreglo. Después, el elemento más
  pequeño se intercambia por el primer elemento del arreglo. El proceso se
  repite para el subarreglo, comenzando con el segundo elemento del arreglo.
  Cada pasada en el arreglo da como resultado a un elemento que se coloca en
  su propia ubicación. Este ordenamiento se desempeña de manera similar al
  ordenamiento de burbuja; para un arreglo de n elementos, es necesario realizar
  n-1 pasos, y para cada subarreglo deben hacerse n-1 comparaciones para
  encontrar el valor más pequeño. Cuando el subarreglo que se está procesando
  contiene un elemento, el arreglo esta ordenado. Escriba la función recursiva
  ordenamientoSeleccion, para desarrollar este algoritmo.
 */

void ordenamientoSeleccion(short arreglo[]) {}
